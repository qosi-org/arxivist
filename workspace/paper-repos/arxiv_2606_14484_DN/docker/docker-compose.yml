version: "3.8"
services:
  quantum_horizon:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ../results:/workspace/results
    command: ["python", "run_all.py", "--config", "configs/config.yaml"]
